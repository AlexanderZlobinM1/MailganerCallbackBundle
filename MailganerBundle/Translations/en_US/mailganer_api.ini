description="Manage Mailganer delivery, packages, reports, domains, stop lists and callbacks."
data="JSON object containing the action parameters."
enable="Enable the Mailganer integration before using its API."
unknown="Unknown Mailganer action. See --help."
dsn="Configure the mailganer+api DSN before using this command."
key="Mailganer API key is missing."
object="--data must be a JSON object."
missing="Missing parameter: %field%"
https="Webhook URL must use HTTPS."
